import React from 'react';
import './CSS/Spinner.css'; // Ensure the correct path for the CSS file

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
